#include<bits/stdc++.h>
#define int long long int
#define fast ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);
using namespace std;

void solve(){

    int n;  cin >> n;
    int x;  cin >> x;
    for(int i = 1; i < n; i++){
        int k;  cin >> k;
    }
    cout << (x == 1 ? "YES" : "NO") << "\n";

}

signed main(){
    fast;
    int t = 1;
    cin >> t;
    while(t--){solve();}
    return 0;
}